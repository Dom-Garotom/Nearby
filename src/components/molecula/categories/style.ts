import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    container: {
        zIndex: 2,
        position: 'absolute',
        top: 20,
        left: 20,
        right: 10
    },

})